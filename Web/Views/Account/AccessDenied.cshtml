@inject IConfiguration configuration;
@{
    Layout = null;
    string Cdn = (await CdnHealthService.GetStaticBaseUrlAsync())?.TrimEnd('/');
    string Ver = configuration["StaticVersion"];
}
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" type="image/x-icon" href="/images/AFSAC.png">
    <title> E-IASAC </title>
     
    <link href="@($"{Cdn}/css/style.css?v={Ver}")" rel="stylesheet" />
    <link href="@($"{Cdn}/css/nunito.css?v={Ver}")" rel="stylesheet"  />
    <link href="@($"{Cdn}/css/roboto.css?v={Ver}")" rel="stylesheet"  />
    <link href="@($"{Cdn}/bootstrap/css/bootstrap.min.css?v={Ver}")" rel="stylesheet"  />
    <link href="@($"{Cdn}/css/login.css?v={Ver}")" rel="stylesheet"  />

    <link href="@($"{Cdn}/sweetalert2/sweetalert2.min.css?v={Ver}")" rel="stylesheet" />
    <link href="@($"{Cdn}/css/feed.css?v={Ver}")" rel="stylesheet" />

    <link href="@($"{Cdn}/css/normalize.min.css?v={Ver}")" rel="stylesheet" />

    <link href="@($"{Cdn}/css/feed.css?v={Ver}")" rel="stylesheet" />
    <link href="@($"{Cdn}/css/main.css?v={Ver}")" rel="stylesheet" />

    <link href="@($"{Cdn}/developercss/accessdenied.css?v={Ver}")" rel="stylesheet" />
</head>

<body>
    <div class="page">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-6 col-lg-6 col-xl-4 bg-white login-wrap align-items-center">
                    <div class="login py-2 py-sm-4">
                        <div class="row">
                            <div class="col-md-10 col-lg-10 col-xl-9 mx-auto">
                                <div class="card-sigin">
                                    <div class="main-signup-header login-form">
                                        <div class="mb-1 logo-sec text-center">
                                            <h1 class="text-danger">Access Denied</h1>
                                            <h6 class="text-danger">You do not have persmission to view this resource</h6>
                                          
                                            <img src="@($"{Cdn}/images/asset/no.gif?v={Ver}")" />
                                        </div>
                                       
                                        
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>



</body>
</html>

