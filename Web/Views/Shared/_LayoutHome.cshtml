@using System.Security.Claims;
@using Web.WebHelpers;
@using Microsoft.AspNetCore.Mvc;
@inject SignInManager<ApplicationUser> signInManager;
@inject UserManager<ApplicationUser> userManager;
@inject IHttpContextAccessor HttpContextAccessor;
@inject IConfiguration configuration;
@{
    string Cdn = (await CdnHealthService.GetStaticBaseUrlAsync())?.TrimEnd('/');
    string Ver = configuration["StaticVersion"];
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <link rel="icon" type="image/x-icon"  href="@($"{Cdn}/images/AFSAC.png")">
    <meta content='width=device-width, initial-scale=1.0, shrink-to-fit=no' name='viewport' />
    @* <meta http-equiv="content-security-policy"content="default-src 'none'; script-src 'self'; connect-src 'self'; img-src 'self'; style-src 'self' 'sha256-CwE3Bg0VYQOIdNAkbB/Btdkhul49qZuwgNCMPgNY5zw=';" /> *@
    <title> E-ISAC </title>
        <!-- Development: Load normal JS -->
    <environment include="Development">
        <link href="@($"{Cdn}/fonts/css/all.min.css?v={Ver}")" rel="stylesheet"   />
        <link href="@($"{Cdn}/css/nunito.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/roboto.css?v={Ver}")" rel="stylesheet"   />
        <link href="@($"{Cdn}/css/DashBoard.css?v={Ver}")" rel="stylesheet"   />
        <link href="@($"{Cdn}/sweetalert2/sweetalert2.min.css?v={Ver}")" rel="stylesheet"   />
        <link href="@($"{Cdn}/css/bootstrap-datetimepicker.min.css?v={Ver}")" rel="stylesheet"   />
        <link href="@($"{Cdn}/css/style.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/extra.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/background.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/typography.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/select2/css/select2.min.css?v={Ver}")" rel="stylesheet"  />
        
@*         <link asp-href-include="~/css/cssStyle.css" rel="stylesheet" /> *@
        <script src="@($"{Cdn}/fonts/feather/feather.min.js?v={Ver}")"></script>
        <script src="@($"{Cdn}/fonts/allfont/webfont.min.js?v={Ver}")"></script>
        <link href="@($"{Cdn}/bootstrap/css/bootstrap.min.css?v={Ver}")" rel="stylesheet"/>
        <link href="@($"{Cdn}/lib/jqueryui/themes/base/jquery-ui.min.css?v={Ver}")" rel="stylesheet" />

        <link href="@($"{Cdn}/toastr/toastr.min.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/main.css?v={Ver}")" rel="stylesheet"  />

        <script src="@($"{Cdn}/lib/jquery/jquery.js?v={Ver}")" ></script>
    </environment>
    
    <!-- Production: Load minified JS -->
    <environment exclude="Development">
        <link href="@($"{Cdn}/fonts/css/all.min.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/nunito.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/roboto.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/DashBoard.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/sweetalert2/sweetalert2.min.css?v={Ver}")" rel="stylesheet"  />
        <link href="@($"{Cdn}/css/bootstrap-datetimepicker.min.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/style.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/extra.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/background.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/typography.css?v={Ver}")" rel="stylesheet" />
       
@*         <link asp-href-include="~/css/cssstyle.min.css" rel="stylesheet" /> *@
        <script src="@($"{Cdn}/fonts/feather/feather.min.js?v={Ver}")" ></script>
        <script src="@($"{Cdn}/fonts/allfont/webfont.min.js?v={Ver}")" ></script>
        <link href="@($"{Cdn}/bootstrap/css/bootstrap.min.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/lib/jqueryui/themes/base/jquery-ui.min.css?v={Ver}")" rel="stylesheet" />

        <link href="@($"{Cdn}/toastr/toastr.min.css?v={Ver}")" rel="stylesheet" />
        <link href="@($"{Cdn}/css/main.css?v={Ver}")" rel="stylesheet" />

        <script src="@($"{Cdn}/lib/jquery/jquery.min.js?v={Ver}")" ></script>
    </environment>

    <link href="@($"{Cdn}/developercss/layouthome.css?v={Ver}")" rel="stylesheet" />
</head>
<body class="nav-fixed">
    @Html.AntiForgeryToken()
    @{
        var userId = this.User.FindFirstValue(ClaimTypes.NameIdentifier);
        var user = await userManager.FindByIdAsync(userId);
        var RoleList = await userManager.GetRolesAsync(user);
        var ClaimsList = await userManager.GetClaimsAsync(user);

        DtoSession? dtoSession = new DtoSession();
        if (!string.IsNullOrEmpty(@HttpContextAccessor.HttpContext.Session.GetString("Token")))
        {
            dtoSession = SessionHeplers.GetObject<DtoSession>(HttpContextAccessor.HttpContext.Session, "Token");

        }
        var RoleName = dtoSession != null ? dtoSession.RoleName : "user";
        
        if (!string.IsNullOrEmpty(@configuration["Footer:Test"]))
        {
            string? Footer = @configuration["Footer:Test"];
            ViewBag.Footer = Footer;
        }
    }
    <div class="d-none" id="postingouthtml"></div>
    <div class="d-none" id="content-container"></div>
    <div class="d-none" id="spnPostedOffrsfornoti"></div>

    <partial name="~/Views/Shared/_CommonHeaderInternal.cshtml" />

    <!-- partial -->
    <div class="page-body-wrapper">


        <!-- partial -->

        <partial name="_Notification" />
        @RenderBody()

        <!-- partial -->
        <!-- main-panel ends -->
        <!-- partial -->
        <!-- main-panel ends -->
    </div>
    <footer class="fixed-bottom">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center">
                    <!-- Invisible placeholder for balancing the center -->
                    <div class="footer-placeholder"></div>

                    <!-- Left Section: Centered Content -->
                    <div class="flex-grow-1 text-center fw-bold">
                        @if (ViewBag.Footer != null)
                        {
                            @(ViewBag.Footer)
                        }
                    </div>
                    <!-- Right Section: Info Spans -->
                    <div class="text-end">
                        <span id="today" class="me-2"></span>
                        <span id="week" class="me-2"></span>
                        <span id="month" class="me-2"></span>
                        <span id="total"></span>
                    </div>
                </div>
            </div>
        </div>
    </footer>
    <!-- page-body-wrapper ends -->
    @*  <script asp-src-include="~/select2/dist/js/select2.min.js"></script>*@
    @RenderSection("Scripts", false)
    @RenderSection("body_scripts", false)

    <script src="@($"{Cdn}/lib/jqueryui/jquery-ui.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/js/bootstrap.bundle.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/bootstrap/js/bootstrap.bundle.min.js?v={Ver}")" ></script>

    <script src="@($"{Cdn}/js/scripts.js?v={Ver}")" ></script>


    <script src="@($"{Cdn}/sweetalert2/sweetalert2.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/toastr/toastr.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/js/moment.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/js/bootstrap-datetimepicker.min.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/developerjs/layout.js?v={Ver}")" ></script> 
@*     <script asp-src-include="~/developerjs/commanvalidation.js"></script> *@
    <script src="@($"{Cdn}/developerjs/pagevalidation.js?v={Ver}")" ></script>
    <script src="@($"{Cdn}/developerjs/_notifications.js?v={Ver}")" ></script>
</body>
</html>


