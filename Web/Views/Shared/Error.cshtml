@inject IConfiguration configuration;
@{
    Layout = null;
	string Cdn = Environment.GetEnvironmentVariable("AFSAC__CDN");
}
@* @if (ViewBag.ErrorTitle == null)
{ *@
<link href="@($"{Cdn}/lib/bootstrap/dist/css/bootstrap.min.css?v={configuration["StaticVersion"]}")" rel="stylesheet" />
<link href="@($"{Cdn}/developercss/error.css?v={configuration["StaticVersion"]}")" rel="stylesheet" />
<section class="page_404">
	<div class="container">
		<div class="row">
			<div class="col-sm-12 ">
				<div class="col-sm-10 col-sm-offset-1  text-center">
					<div class="four_zero_four_bg">
						<h1 class="text-center ">404</h1>


					</div>

					<div class="contant_box_404">
						<h3 class="h2">
                            An occured while processing your request.
                            The support team is notified and we are working on the fix.
						</h3>

                        <p>In case of technical issues, please contact Admin of IT Cell.</p>

						<a href="/Account/IMLoginSelf" class="link_404">Go to Login</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

 
@* }
else
{
    <h1 class="text-danger">@ViewBag.ErrorTitle</h1> 
    <h6 class="text-danger">@ViewBag.ErrorMessage</h6>
}
 *@