@inject IConfiguration configuration;
@model List<DTOBasicDetailTempRequest>
@{
    string Cdn = Environment.GetEnvironmentVariable("AFSAC__CDN");
}
<partial name="~/Views/Shared/_DataTableCSS.cshtml" />
<header class="page-header page-header-dark bg-gradient-primary-to-secondary pb-10">
    <div class="containerMain">
        <div class="page-header-content">
            <div class="row align-items-center justify-content-between">
                <div class="col-auto">
                    <h1 class="page-header-title">
                        <div class="page-header-icon"> <i class="fa fa-bars"></i></div>
                        <a href="/Home/Index" class="mr-2">Home</a> > <a href="/Home/Dashboard" class="mr-2">Dashboard</a>  > Incorrect Details/Data


                    </h1>
                </div>
            </div>

        </div>
    </div>
</header>
<div class="containerMain mt-n10">
    <div class="card mb-3 ">
        <div class="card-header">
            <div class="row">
                <div class="col-xl-12 text-left">
                    <h3>
                        @ViewBag.Title
                        <small class="text-muted"></small>
                    </h3>
                </div>
            </div>
        </div>
        <div class="card-body card-body-min-height">
                <div class="row">
                <span id="spnhdns" class="d-none">@ViewBag.hdns</span>
                    <div class="col-xl-12">
                    <table id="tbldatatabledata" class="table table-striped table-hover border border-purple">
                        <thead>
                            <tr>
                                <th>@Html.DisplayNameFor(model => model[0].Sno)</th>
                                <th>Army No</th>
                                <th>Name</th>
                                <th>Created On</th>
                                <th>Observation</th>
                                <th class="noExport">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                string[] rem = null;
                                if (item.Remarks2 != null)
                                    rem = item.Remarks2.Split("#");

                                <tr>
                                    <td>@item.Sno</td>
                                    <td>@item.ServiceNo</td>
                                    <td>@item.RankName @item.FName @(item.LName == null ? "" : item.LName)</td>
                                    <td>@item.UpdatedOn.ToString().Replace("-", "/").Replace("00:00:00", "")</td>
                                    <td>
                                        <ul>
                                            @if (rem != null)
                                            {
                                                foreach (var data in rem)
                                                {
                                                    <li>@data</li>
                                                }
                                            }
                                        </ul>
                                    </td>
                                    <td>
                                        <a asp-controller="BasicDetail" asp-action="InaccurateDataView" asp-route-Id="@item.EncryptedId" class="btn btn-primary mr-2 ">View Details</a>
                                        <span id="RegistrationApplyFor" class="d-none">@item.RegistrationId</span>
                                        <span id="OffType" class="d-none">@item.ApplyForId</span>
                                        <span id="lCardType" class="d-none">@item.TypeId</span>
                                        <span id="ArmyNo" class="d-none">@item.ServiceNo</span>
                                        @* <button class="cls-btnRetry btn btn-success" id="btnRetry">Retry</button> *@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    </div>
                </div>
            </div>
       
    </div>
</div>
@section Scripts{
    <script src="@($"{Cdn}/developerjs/inaccuratedata.js?v={configuration["StaticVersion"]}")"></script>
    <partial name="~/Views/Shared/_DataTableJS.cshtml" />
}
